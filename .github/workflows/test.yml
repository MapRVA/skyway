name: CI

on:
  pull_request:
  push:
    paths:
      - skyway/**
      - Cargo.toml
      - Cargo.lock
      - .github/workflows/test.yml
    branches: [trunk]

jobs:
  cargo-test-linux:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    name: "tests | Ubuntu (x86_64)"
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - uses: rui314/setup-mold@702b1908b5edf30d71a8d1666b724e0f0c6fa035 # v1

      - uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0 # v2.8.0

      - name: "Install Rust toolchain"
        run: rustup show

      - name: "Cargo test"
        run: cargo test

  cargo-test-macos-aarch64:
    timeout-minutes: 15
    runs-on: macos-15
    name: "tests | MacOS (aarch64)"
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - uses: rui314/setup-mold@702b1908b5edf30d71a8d1666b724e0f0c6fa035 # v1

      - uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0 # v2.8.0

      - name: "Install Rust toolchain"
        run: rustup show

      - name: "Cargo test"
        run: cargo test

  cargo-test-windows-x86_64:
    timeout-minutes: 10
    runs-on: windows-latest
    name: "tests | Windows (x86_64)"
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0 # v2.8.0
      - name: "Install Rust toolchain"
        run: rustup show

      - name: "Cargo test"
        run: cargo test
