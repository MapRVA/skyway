use osmpbf::{Element, ElementReader, TagIter};
use std::path::Path;

pub fn load_pbf(src: &Path) {

    src;
    // return ElementReader::from_path(src.to_string_lossy())
    //     .expect("input .pbf file is not readable");
}
